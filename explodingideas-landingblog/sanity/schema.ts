// Import your custom types
import { schemaTypes as mySchemaTypes } from '../schemas/index' // adjust the path as needed

// Export your schema
export default {
  name: 'default',
  types: mySchemaTypes
}
